# Leave blank when the frontend is served by the backend from the same origin.
# For a separate frontend deployment, use the backend host. /api is added automatically.
VITE_API_URL=

# Leave blank for same-origin Socket.IO. For a separate backend deployment, use its root URL.
VITE_SOCKET_URL=
